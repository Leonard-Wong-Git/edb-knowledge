{
  "name": "edb-knowledge-platform-backend",
  "version": "1.5.0",
  "private": true,
  "type": "module",
  "description": "Knowledge RAG Platform backend for injecting approved EDB knowledge into circular analysis prompts.",
  "scripts": {
    "dev": "tsx --env-file=.env src/server.ts",
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "regression:semantic": "tsx scripts/semanticRegression.ts",
    "embeddings:build": "tsx --env-file=.env scripts/buildFactEmbeddings.ts",
    "embeddings:build:dry": "tsx scripts/buildFactEmbeddings.ts --dry-run",
    "embeddings:verify": "tsx scripts/verifyFactEmbeddings.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "node-fetch": "^3.3.2",
    "openai": "^4.104.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.8.2"
  }
}
