Work in /Users/leonard/Downloads/Claude Project/Claude-edb-knowledge/Draft. Read AGENTS.md, then dev/SESSION_HANDOFF.md. Trust the handoff over this generated mirror.

If the root does not match the handoff, stop and ask for confirmation. Do not read dev/SESSION_LOG.md during ordinary startup. Read dev/PROJECT_INDEX.md, dev/RULE_PACKS.md, dev/DOC_SYNC_REGISTRY.md, dev/CODEBASE_CONTEXT.md, dev/PROJECT_MASTER_SPEC.md, and task packs only when the current task requires them.
所有路徑含空格，終端機指令必須用雙引號包住。

一個單獨的「開工」/「Start Agent Handoff」只授權：最小狀態復原 → 起手探針 → 起手卡 →
報告當前目標／風險／建議下一步，然後結束該回合。同一則訊息若帶任務則照常開始。

--- 專案狀態（S227, 2026-09-16）---

平台 v3.3.8；Supabase 17,004 chunks；in-app 瀏覽庫 170 份；guidelines.json 公開端點 151 份
（_meta 2.6.2）；knowledge.json 仍 _meta 2.3.0 · facts 455。Agent Handoff Kit v0.3.66。
FEATURE_ROUTE_FIRST_SEARCH=1 在 Render 啟用（S226 已用行為探針核實：185 題生產 run 與本機
旗標開啟側 184/184 逐條同判）。另三個量度 knob 生產一律未設＝關閉，且都不應啟用。
git：開工必先 fetch 再比對，不要信這裡記的 hash。
線上 backend 有沒有落後【比檔不比 hash】：git diff --name-only <部署commit>..HEAD -- backend

🟢 S227 做完了甚麼（都不用重做）：
   1. 答案層 rubric 判官已交付 —— backend/scripts/_s227_rubricJudge.ts（OP① 完成）。
      pairwise、每題兩個次序各判一次、判讀門檻寫死在碼內；67 條 self-test、七次紅測。
   2. FEATURE_LEXICAL_RERANK 已判定【不出貨】：38 題 net −1（門檻 8），25 題答案毫無差別。
      穩健性先算過：6 條須人手覆核題全算對手贏也只到 +5，翻不了盤。
   3. 判官本身要先驗才可信 —— 三輪校準證明 gpt-4.1-mini 是壞尺（14 次判決 11 次揀先出現
      那一個、TIE 零次），gpt-4.1 才把位置偏誤歸零。用它必須 JUDGE_MODEL=gpt-4.1。
   4. staff_appraisal 路由已拆出並上生產（PR #20 → 7be4404）。S226 的「評核」修正只醫好
      一半：路由改了，但 hr_admin 的 QUERY_EXPANSIONS 是假期詞彙，把答案段落由第 1 推到
      第 9。拆出後不設展開詞，生產實測答案段落（SAG §7.7 p.208）入窗第 5、八格全部考績
      來源、一條假期都沒有。
   5. 新工具 dev/source/route_blast_radius.mjs —— 改路由必跑，掃 185 條 gold query 對照
      任一 git ref，補上 route_regression.mjs（54 條人手案例）覆蓋不到的範圍。
   6. 「查詢展開拉偏」已證不是系統性問題（本節自行收回該提議）：37 條有展開的題目中，
      展開幫 29、害 8，中位 +0.0838，只有 1 條因展開跌出窗。不要去改展開機制本身。

⚠️ 未解決（不要當已解決）：
   · 片段層 recall@8 0.349 本體（OP②）。已排除四條路：不是切片問題、調配額是死路、
     詞面重排答案層無得益、改展開機制本身。未試只剩：cross-encoder 重排、按查詢長度
     自適應（plain 1–3 詞題 FAIL 24/77 vs natural_sentence 6/43）。
   · OP⑩ hr_admin 的「津貼」token 搶走 kg_relocation_grant（「幼稚園搬遷津貼」），與
     S227 修好的那條同族。動它前必須先量 blast radius，會牽動 hr_ncs_allowance 等題。
   · 封版閘兩個 ERROR 只能靠改善檢索清走；兩個 WARN 未有 waiver；六項人手驗證從未記錄。
   · 1 條 CONFIDENT_WRONG（分數 0.7575 > 棄權門檻 0.75）＋ 8 條查詢引用 gold 明令禁引的
     來源，全部是「同一指引的不同對象／學級版本」。
   · sch_bus 五個對象版本吃掉 69 個窗格 —— 共用配額已證不成立，要處理的是那五份文件
     該不該各自入庫與瀏覽（產品決定）。
   · footnote_lead_probe.py 與 judge_probe.py 未跑（DOC_SYNC row 要求，啟用重排前要補）。
   · backend/scripts/*.ts 十二支工具不在 npm run check 覆蓋範圍（tsconfig include 只有
     src/**）—— 要單獨指檔才真正 typecheck。Backlog ⑨。

⚠️ 量度紀律（違反其中任何一條都會得出錯的結論）：
   · 用 _s226_captureQueryVec.ts 做前後對照，必須由外面設 FEATURE_ROUTE_FIRST_SEARCH=1，
     否則量的是生產不執行的配置（S227 中過一次，量到的窗完全不同）。
   · 本機比 Render 慢 1.5–2 秒；anon 角色 statement_timeout 只有 3 秒而 service key 是 8 秒，
     本機 in-process 慣例會 fallback 到 service key，系統性低估失敗率。
   · 實例層冷熱落差 58–90 倍，任何兩組查詢的比較都要交錯重量三輪以上。
   · 判官在準則 3（數字）兩個方向都出過錯，凡依賴該準則的判決自動標 needs_human_review。

⚠️ 不在 git 內的外部設定（換機或重裝要記得）：
   repo 開了 allow_auto_merge + delete_branch_on_merge；deploy key id=163258294；
   repo secret LEDGER_DEPLOY_KEY；ops repo edb-knowledge-ops 的 executor.yml 有 GH_TOKEN。
   語料快取 dev/source/.store_cache.json 是 28MB 生成檔、已 gitignore，換機要重建：
   set -a && . backend/.env && set +a && python3 dev/_s213_recon.py --cache dev/source/.store_cache.json

Post-startup first action: 先做起手探針（served app.html PLATFORM_VERSION 應為 3.3.8 +
Render /health 應報 commit 7be4404 + git fetch 後比對 HEAD/origin/main + Supabase live count
應為 17,004 + check_registry_drift --check 的 ZOMBIE 應為 0）。判斷線上 backend 有沒有落後
要【比檔不比 hash】。之後就 OP② 剩下的兩條未試方向（cross-encoder 重排、按查詢長度自適應）
向 Leonard 提一個帶成本估算的建議 —— 判準已經有，量完答案層再決定出不出貨。
未得明確批准，不得改任何 flag、不得執行 DDL、不得刪除或寫入生產片段、不得作外部模型批次。
