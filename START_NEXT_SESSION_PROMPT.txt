Work in /Users/leonard/Downloads/Claude Project/Claude-edb-knowledge/Draft

Read AGENTS.md first (governance SSOT), then follow its §1 startup sequence:
dev/SESSION_HANDOFF.md → dev/SESSION_LOG.md → dev/CODEBASE_CONTEXT.md → dev/PROJECT_MASTER_SPEC.md
(Playbook lazy: read only "Leonard's playbook/playbook/INDEX.md"; open a card only on trigger.)

Current state (S189, 2026-06-28): 平台 v3.2.2; Supabase 15,838 chunks; source_registry 242;
HEAD==origin/main (S189 latest closeout commit); 凍結合約 _meta 2.3.0 / facts 455 / guidelines 158;
4 個自動監察 active (discover / freshness / served-url / new-circular); 0 outstanding bug.

Option A 自動入庫管道進度 (4 phase):
- ✅ Phase 1 (S188): 入庫包生成器 dev/source/prepare_ingest_package.py (staging-only).
- ✅ Phase 2 (S189): dry-run executor dev/source/execute_ingest.py + 批准格式 (dev/source/ops/approvals/)
  + ops repo scaffold dev/source/ops/ (gitignored, 未來 private edb-knowledge-ops 種子). 全 staging-only
  零 live 寫入. executor 模擬 6 步 (copy→vault / registry-append / chunk / route-patch / display-sync /
  commit) 寫 execution_plan.json; approval gate (decision==approved 先得) + human overrides; live 模式
  hard-refuse exit 2 (Phase 3 未 wired). 3 staged package 已備 (edbc007 approved-demo / edbcm077 / edbcm101).

🔜 NEXT (待 Leonard): Phase 3 = wire execute_ingest.py --live (真做 6 步 + commit/push, approval gate +
post-deploy smoke + 失敗 re-open). 前置 (需 Leonard 雙手, agent 做唔到): 開 private repo
Leonard-Wong-Git/edb-knowledge-ops + fine-grained PAT MAIN_REPO_PAT (contents:RW on edb-knowledge) +
Actions secrets SUPABASE_SERVICE_KEY / OPENAI_API_KEY + 揀 trigger — 完整清單見 dev/source/ops/README.md.
然後 Phase 4 = ops/.github/workflows/executor.yml 排程端到端. (可同 S187 安全 backlog repo 私有化方向合一.)

其他 backlog: S187 安全 (repo private+hosting 搬遷 / Supabase RLS-off+anon SELECT / sibling repo
EDB-AI-Circular-System 待審); S186 2 源 monitor (edbcm073 電子學習撥款 + edbcm066 準英語教師獎學金,
in-route 但短 query 排名低, Leonard 報 miss 先 boost); Feature 2a 追問 + 2b 文件 scoped Q&A;
Phase 3 full_chunks_routed (+4 backend route). 監察: SESSION_LOG §4a 維護 script 個 latest-prompt 檢查
仍 grep 舊 heading「Next Session Handoff Prompt (Verbatim)」、本檔已用「Next Session Opening Message」
(AHK 慣例) → script exit 1 屬 stale-assertion false-positive (archive 操作本身正確), 唔阻 closeout.

Post-startup first action: 跑起手探針 (served app.html PLATFORM_VERSION 3.2.2 + Render /health warm 455
+ Draft HEAD==origin/main + Supabase chunk count), 然後向 Leonard 報告當前狀態同建議下一步.

所有路徑含空格, 終端機指令必須用雙引號包住. 改任何嘢之前, 先報告當前狀態同建議下一步.
