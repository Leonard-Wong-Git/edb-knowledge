Work in /Users/leonard/Downloads/Claude Project/Claude-edb-knowledge/Draft

Read AGENTS.md first (governance SSOT), then follow its §1 startup sequence:
dev/SESSION_HANDOFF.md → dev/SESSION_LOG.md → dev/CODEBASE_CONTEXT.md → dev/PROJECT_MASTER_SPEC.md
(Playbook lazy: read only "Leonard's playbook/playbook/INDEX.md"; open a card only on trigger.)

Current state (S190, 2026-06-29): 平台 v3.2.2; Supabase 15,874 chunks; source_registry 244;
HEAD==origin/main (S190 latest closeout commit); ops repo Leonard-Wong-Git/edb-knowledge-ops (private);
凍結合約 _meta 2.3.0 / facts 455 / guidelines 158; 0 outstanding bug.
自動化 active: 4 源監察 (discover / freshness / served-url / new-circular) + Option A 管道
(option-a-approvals 每日 19:30 HK refresh Issue + @mention-on-new ping; option-a-executor cron 20:00 HK + workflow_dispatch).

✅ Option A 自動入庫管道 4 phase 全 DONE + 端到端 VERIFIED LIVE + OPERATIONAL:
偵測 → prepare_ingest_package.py 出包 → 私密 Issue「📥 待批准入庫候選」剔 box (或 /approve <num> route=...)
→ approval-issue.yml 寫批准 + dispatch executor → execute_ingest.py --live (6 步: copy-vault / registry /
ingest embed+INSERT / route-patch / display-sync / commit+push) + 自動寫 update_log → Render+Pages redeploy.
2 次真入庫已驗: edbc007 (manual run) + edbcm096 (Issue 剔掣). 三重閘: approval + secrets pre-flight + idempotent/resumable.
日常 hands-off: 19:30 有新通告先收 @mention ping → 手機剔 box → ~2-5 分鐘自動入庫+部署. cron 兜底.

🔜 NEXT (待 Leonard 揀方向):
  ① Circular System 安全審計 (Leonard 下個焦點): sibling repo EDB-AI-Circular-System (circular.wongfu.net),
     亦 public, 用 PolicyChecker S187 同級 rigor 審 (secrets/git history/暴露面/寫入面/rate-limit/anon 權限).
  ② S187 PolicyChecker 安全 backlog 餘項: repo 轉 private + hosting 搬離 GitHub Pages (同 ops private-repo 合一) /
     Supabase 開 RLS + 收 anon RPC-only (需 Supabase SQL) / GitHub Issues world-readable (low).
  其他 backlog: Option A 低優先 follow-up (write-back annotate ops approval; refresh checkbox-reset cosmetic);
  S186 2 源 monitor (edbcm073 電子學習撥款 + edbcm066 準英語教師獎學金, 短 query 排名低, 報 miss 先 boost);
  Feature 2a 追問 + 2b 文件 scoped Q&A (Leonard S182 揀 sequence A); Phase 3 full_chunks_routed (~60 chunk + 4 route);
  VE planning tools 4 PDF; source_registry 26 新源 metadata fold-in; index.html nav 品牌統一 (1-line);
  DEBP image-PDF OCR (playbook doc-extract-method-ladder + inbox OCR 提案).

✅ Playbook 已沉澱 (S190): private-ops 控制平面 + Issue 剔掣批准 pattern 入共用庫 inbox (pushed), 可套其他 system.

Post-startup first action: 跑起手探針 (served app.html PLATFORM_VERSION 3.2.2 + Render /health warm 455
+ Draft HEAD==origin/main + Supabase chunk count 15,874), 然後向 Leonard 報告當前狀態同建議下一步.

所有路徑含空格, 終端機指令必須用雙引號包住. 改任何嘢之前, 先報告當前狀態同建議下一步.
