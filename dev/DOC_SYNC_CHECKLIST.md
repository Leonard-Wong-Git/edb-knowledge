# Doc Sync Checklist
<!-- LOCAL PROJECT RECORD -->
<!--
  USAGE: At PERSIST phase, if any file was created or modified during CHANGE:
  1. Identify the change category in the registry below
  2. Execute all "Required Doc Updates" for matched rows
  3. Record triggered rows in SESSION_LOG under "Doc Sync"
  4. If your change type has no matching row: add the row first, then proceed
     (prevents this registry from going stale)
-->

## Change Category Registry

| Change Category | Required Doc Updates | Verification Method |
|---|---|---|
| Governance rule change (AGENTS.md) | INIT.md FILE 1 mirror; README if behavior is user-facing | grep parity check |
| Tech stack / build / dependency change | CODEBASE_CONTEXT.md Stack or Build section | manual review |
| External API / service change | CODEBASE_CONTEXT.md External Services block | block format check |
| New governance file added to install | §5a backup list in AGENTS.md; INIT.md ROOT SAFETY CHECK backup list; INIT.md FILE 1 §5a | grep check |
| Session-log maintenance utility added/changed | AGENTS.md §4a mechanism enforcement; INIT.md FILE 7 + FILE 1 §4a + §5a backup list; README*.md safeguards section; docs/qa/run_checks.sh | script self-test + grep |
| New project doc added | This file — add a row for the new doc's update triggers | row presence check |
| Governance bootstrap / INIT execution | SESSION_HANDOFF.md Last Session Record; SESSION_LOG.md task entry + handoff prompt | manual review |
| Product behavior / tuning change | SESSION_HANDOFF.md baseline, priorities, risks if affected; SESSION_LOG.md task entry + QC evidence | manual review |
| Product version / release milestone change (user-facing `PLATFORM_VERSION`; **decoupled from frozen data contract since S163**) | app.html `PLATFORM_VERSION` constant; README badge + footer; CHANGELOG new section; SESSION_HANDOFF.md baseline/priorities; SESSION_LOG.md task entry; CODEBASE_CONTEXT.md AI Maintenance Log. **Do NOT bump frozen `knowledge.json` / `role_facts.json` `_meta.version`** (data contract stays 2.3.0; downstream Circular System contract unchanged) | headless app.html boot shows new version + no stale old version in render |
| Backend README / standalone runbook added | CODEBASE_CONTEXT.md Build & Run or Directory Map; SESSION_HANDOFF.md priorities if operator flow changes; SESSION_LOG.md task entry + QC evidence | manual review |
| Knowledge operating architecture / planning doc | CODEBASE_CONTEXT.md Directory Map or Key Decisions if it changes long-term direction; SESSION_HANDOFF.md priorities/risks if follow-up work changes; SESSION_LOG.md task entry + QC evidence | manual review |
| _[Add project-specific rows below this line]_ | | |
| **Tab withdraw / restore**（`window.FEATURE_TABS` in app.html head — 收起或恢復任何一個 tab）| app.html：tab 掣 / `VALID_VIEWS` / `channels` 卡 / 使用手冊摺疊 / `TOUR_STEPS`；mobile.js `buildTabBar()`；index.html 對應 feature card 嘅 inline style（**唯一唔係 flag 驅動嘅位**）；README 功能表若提及該 tab；CHANGELOG 記一筆 | 真站 headless 驗：該 tab 名喺 `document.body.innerText` 零出現、`[role="tab"]` 清單冇佢、手機底欄格數正確、`#<view>` 直入會 fallback 去 qa |
| **Option A 自動入庫管道改動**（`dev/source/prepare_ingest_package.py` / `execute_ingest.py` / ops repo `edb-knowledge-ops` 嘅 `executor.yml`+`approval-issue.yml`+批准格式 — S188-S190 建、S193 加可見度閘）| `dev/source/execute_ingest.py` docstring 步驟表（步驟增減必同步）；`dev/source/ops/README.md` + `APPROVALS_FORMAT.md`（若批准格式／secrets／workflow 契約變）；`dev/CODEBASE_CONTEXT.md` Directory Map 該檔描述 + AI Maintenance Log；`dev/SESSION_HANDOFF.md` Current Baseline（管道狀態）+ Open Priorities（follow-up）；`dev/SESSION_LOG.md` task entry + QC evidence。**若改動涉及 backend `searchChannelB.ts` 嘅機器插入點**（`ack:spotlight` marker / SOURCE_SETS 插入格式）：marker 必保留，並喺 code 註解寫明係 executor 插入點 | `py_compile` ✅；`--dry-run` 對真 staged package 跑通且**批准閘仍擋**（`decision != approved`）；**零 live 寫入核實**（registry count / knowledge chunks / 無新 vault dir / `git diff` 乾淨）；改 live 步驟後首次自動入庫要覆核 bot commit + Supabase 實數 + 可見度 smoke（rank 而唔係 bool）|
| New / iterated isolated PoC (Testing/ only, no Draft code/data/contract change) | SESSION_LOG/HANDOFF record at closeout; the PoC's own Testing/ README; CODEBASE_CONTEXT N/A (Testing/ is not Draft tech-stack/dir; PoC unpromoted) | Draft `git status` shows zero PoC files outside Testing/; only governance docs changed |
| Long-term spec / locked decision / architecture invariant change | dev/PROJECT_MASTER_SPEC.md (relevant §A–§G section); CODEBASE_CONTEXT.md Key Decisions if direction shifts; SESSION_HANDOFF.md if baseline affected | manual review |
| New cross-agent handoff knowledge doc added | CODEBASE_CONTEXT.md Directory Map + AI Maintenance Log; this registry (row presence); SESSION_HANDOFF/LOG | row presence check |
| Project relocation / repo absolute-path change | AGENTS.md header line 1 + §13 examples; SESSION_HANDOFF.md User Environment + Session Close Checklist; PROJECT_MASTER_SPEC.md §A.5; any *.py/*.sh with hardcoded abs path hints; SESSION_LOG/HANDOFF entry | grep "old path" returns only archive/log history |
| Doc-drift truth-pass / accuracy correction (a doc number/architecture statement found wrong vs actual code/data) | Correct every doc carrying the stale value (PROJECT_MASTER_SPEC / CODEBASE_CONTEXT / SESSION_HANDOFF as applicable); CODEBASE_CONTEXT AI Maintenance Log entry; dev/HANDOFF_PACKAGE.md §2/§5 if a verified-state value changed; SESSION_LOG drift table | re-verify corrected value against actual code/data |
| Monitoring / CI workflow change (Method A `check_freshness.py`/`freshness_check.yml`/`freshness_metadata`; discovery `discover_sources.py`/`discover_check.yml`; **Method B served-URL `check_served_urls.py`/`served_url_check.yml` — S172**) | dev/source/FRESHNESS_GUIDE.md (§0 two-monitor model + rhythm + commands + detection model); CODEBASE_CONTEXT.md Directory Map if a script/workflow/field is added; SESSION_HANDOFF.md priorities/risks if operator flow changes; SESSION_LOG.md task entry + QC evidence; **note any new CI secret dependency (e.g. `SUPABASE_ANON_KEY` for served-URL check)** | `--self-test` PASS + dry-run/`--check` smoke; YAML parse |
| guidelines.json public contract / app.html GUIDELINES_REGISTRY change (the public endpoint is a derived projection of the registry — S140) | Regenerate `guidelines.json` via `dev/build_guidelines.py --write` (NEVER hand-edit); K1_API_SPEC.md (root + dev/) count/version; README.md guideline count; PROJECT_MASTER_SPEC §B.1 釐清框 if subset/drop policy changes; CODEBASE_CONTEXT.md guidelines.json line; SESSION_LOG/HANDOFF task entry | `build_guidelines.py --self-test` PASS (incl. no-prior-public-id-lost regression guard); JSON valid; per-topic counts |
| Channel-B vault source backfill / page-carry into Supabase (new or refreshed source via repage_pdfs.py + cb3_b2_pagecarry_migrate.py — S122-S141 recurring) | source_registry.json entry (source_type=pdf + direct url_primary + freshness_metadata); backend SOURCE_SETS / TOPIC_KEYWORDS / QUERY_EXPANSIONS parity if topic-routed (S135 backfill-allowlist coupling — new Supabase source does NOT surface until allowlisted); SESSION_HANDOFF.md Current Baseline (Supabase chunk count + marker-bearing count); SESSION_LOG.md task entry + Gate1/Gate2 evidence; CODEBASE_CONTEXT.md Directory Map (new vault dir) + AI Maintenance Log | repage Gate1 markers==pages; cb3_b2 Gate2 post-count==insert (del=0 for new source); typecheck+build exit 0; live smoke surfaces source with `=== Page N ===` + curriculum-route non-regression |
| Channel B sync contract / endpoint change (`CHANNEL_B_SYNC_SPEC.md`; manifest/fetch endpoints — Q4 Phase 2 incremental-sync) | `dev/CHANNEL_B_SYNC_SPEC.md` (the contract); backend new routes + env `CHANNEL_B_SYNC_KEY` when built; PMS §C.4 (Supabase facts) + §F.2 (Q4 decision chain); CODEBASE Directory Map + Key Decisions + AI log; SESSION_HANDOFF/LOG | spec internal-consistency vs live `wiki_chunks` schema; when built: typecheck+build exit 0 + manifest/fetch contract smoke + key-gate 401/403/503 |
| **Synthesis 前置閘改動**（`searchChannelB.ts` 嘅 `FOOTNOTE_LEAD_SCORE` / `FOOTNOTE_LEAD_MIN_OVERLAP` / `VAULT_LEAD_SCORE` / judge bypass 條件 / footnote 同 spotlight overlay 嘅 lead slot — S174/S177/S178/S183/S193/S196）| `dev/source/footnote_lead_probe.py`（footnote lead 的驗收工具，S196 建）；`dev/source/judge_probe.py`（vault 門檻的驗收工具，S195B 建）；常數旁邊的實測註釋（數字必須連同量度到的分佈一齊寫，唔可以淨寫一個數）；`CODEBASE_CONTEXT.md` Directory Map（新工具／新 lib）；SESSION_LOG task entry + before→after 證據；SESSION_HANDOFF Risks 若閘的行為邊界有變 | **改呢類閘唔可以只靠 eval**：eval 量度檢索，呢啲閘量度「答唔答」。必須 (a) `footnote_lead_probe.py --run` 一對 before→after，positive control 零損失；(b) 涉及 vault 門檻則 `judge_probe.py`；(c) 全語料覆核而唔止抽樣；(d) 失效方向必須 fail-open（量度唔到就維持舊行為）。S196 教訓：offline 校準過關唔代表得，deploy 完要 live 重探（bigram 分唔開 營辦商／承辦商 就係 live 先捉到）|
| **store chunk 退出服務路徑（by chunk id）**（`searchChannelB.ts` 的 `RETIRED_MIRROR_CHUNK_IDS` / `retiredMirrorFilter` — S197 建，用嚟令個別已入庫 chunk 唔再出現喺答案，而唔使喺 Supabase 刪行）| 每個新 id 旁邊必寫**已核實嘅替代出處 + 頁碼**（唔可以只寫 id）；`dev/source/CHANNEL_A_RETIREMENT_LEDGER.tsv` 對應行；`CHANNEL_A_COVERAGE_FINDINGS.md`（判斷理由）；`CODEBASE_CONTEXT.md` Directory Map 該檔描述；SESSION_LOG task entry + before→after 證據；SESSION_HANDOFF Risks 若「答案必有出處」護欄狀態有變 | **唔准由總帳／任何自動 tier 直接延長個 list** —— S197 實測：機械判定嘅 `CLEARED` tier 有 **44%**（7/16）撐唔住人手覆核（替代品講另一科目／用定義冒充規則／實質有但職責歸屬冇／替代品係問卷通告），每條必須開段落親眼讀；`eval_retrieval.py` 一對 before→after （**先確認 eval 集覆蓋到被剷內容嗰個維度** —— S197 原 30 條對「邊個負責」完全盲，剷咗會量到假綠燈）；live 抽驗剷咗嘅 query 改由有頁碼嘅來源服務、而保留嗰批仍然出到；tsc exit 0 |
| **Per-chunk deep link（`wiki_chunks.url` 逐條指子頁 / 附件，唔係 source landing 頁）** —— `source_registry.json` 嘅 `section_urls` opt-in map + `expand_vault.py` 嘅 `carry_sections` 派 URL（S207 建） | `source_registry.json` 該源 `section_urls`（label → 絕對 URL，**逐個寫實，唔准 base+suffix 推導**）+ notes 記低「唔准 `--fetch`」（多子頁 crawl 嘅 extract 一 refetch 就淨返 landing 一頁）；`SESSION_HANDOFF.md` Current Baseline（受影響 chunk 數 + 帶 deep link 數）；`SESSION_LOG.md` 帶 HEAD-verify 證據；如同時改 chunk 文字 → 一併走 row 37 | **入庫前逐條 HEAD 200，fail closed（一條唔過 → 成個 source skip）**；HEAD ≥400 要再試 GET 先落判（有站拒 HEAD）；`dev/vault/test_carry_rules.py --self-test` 全綠 + `--prove-assertions` 證測試會紅；重入後實查線上 distinct URL 數 == map 覆蓋數；eval before→after 0 blocking failure；**per-chunk URL 冇任何監察覆蓋 —— served-url 監察只讀 registry `url_primary`，呢個缺口未補（見 Open Priorities）** |
| **檢索 eval harness 改動**（`dev/source/eval_retrieval.py` / `dev/source/eval_queries.json` / `dev/source/eval_runs/*` — S194 建，roadmap R1）| `dev/source/eval_queries.json`（query set 或 `expect_any` 斷言變動＝改緊 regression 的定義，必同 pass 記入 SESSION_LOG 並講明理由）；`CODEBASE_CONTEXT.md` Directory Map（旗標／檔案增減）；`dev/source/eval_runs/` 保留對照 baseline run；SESSION_HANDOFF baseline 若 PASS/FAIL 數變；任何檢索改動（SOURCE_SETS / TOPIC_KEYWORDS / QUERY_EXPANSIONS / spotlight / supersede / 門檻）必須有一對 before→after run 作證據 | `--self-test` 全綠；`--compare before after` 無 SET_LOST／VERDICT_REGRESSED／ERROR，否則必須逐條解釋（tie flip 已由 `_tie_aliases` 吸收，唔算回歸）|
| **Anti-confab judge prompt 改動**（`searchChannelB.ts` 的 `RELEVANCE_JUDGE_PROMPT` 本身 — 唔係 bypass 門檻，門檻走上面「Synthesis 前置閘改動」row；S199 建驗收機制）| `dev/source/judge_acceptance.py` + `judge_acceptance_cases.json`（凍結驗收集，**改 prompt 前後各跑一次**，run 存 `dev/source/judge_runs/`）；`dev/source/JUDGE_PROMPT_FINDINGS.md`（量到嘅數＋點解 ship／唔 ship）；`CODEBASE_CONTEXT.md` Directory Map；SESSION_LOG 記 before→after 兩個數；SESSION_HANDOFF Risks 若 judge 行為邊界有變 | `--self-test` 全綠；`--check-parity` 證明 harness 入面嗰份 prompt 同 `searchChannelB.ts` 逐字相同；**`--plumbing-check` 必跑**（證明個 harness 叫得出「能」，否則一片「否」可以係工具壞咗而唔係 judge 判嘅 — S198 沉默陷阱）；**false answer 同 accuracy 分開報**，decline 半邊出現任何一個 false answer ＝唔准 ship，`D00_s177_frozen_post` 中招更加係一票否決；ship 後必須重跑 `footnote_lead_probe.py`（judge 肯答多咗，lead gate 守緊嘅嘢就變咗）|
| New user-facing feature (new public backend endpoint + frontend surface; e.g. 文件分析 S154) | README.md 功能簡介 table + tab-count note; CODEBASE_CONTEXT.md Directory Map/Key Entry Points + Stack (if new CDN/dep) + AI Maintenance Log; SESSION_HANDOFF.md baseline + priorities + risks; SESSION_LOG.md task entry + QC evidence; K1_API_SPEC ONLY if the downstream static-JSON contract changes (new app endpoints alone do NOT touch it) | typecheck+build exit 0; live render 0 console error; existing-endpoint regression smoke; post-deploy live smoke |
| 範疇要求清單 derived artifact（`dev/checklists/*.md` — 由 Supabase Channel B 來源蒸餾；S155 試點 採購及財務） | Regenerate / review the checklist when any member source re-ingests (freshness 週跑 flags it) or its SOURCE_SETS membership changes; SESSION_HANDOFF/LOG task entry; CODEBASE_CONTEXT Directory Map only when checklists become a published product surface (dev-only draft 階段唔使) | every item carries 引文＋頁碼 and passes mechanical re-verify (exact/ws/NFKC substring + nearest-page recompute); source URLs return 200 PDF |
| 學校版 docx / checklist+clauses 重生 → 派生 manifest 同步（S160：政策範本 tab + 文件修訂 feature 依賴） | 改 `dev/checklists/<域>/*.docx` 後 re-run `dev/checklists/_work/gen_templates_manifest.py`（→`policy_templates.json`，政策範本 tab）；改 `_work/<域>/{checklist,clauses}.json` 後 re-run `gen_checklists_bundle.py`（→ root `checklists_bundle.json`，backend `/api/checklist-revise` 載入）；兩個 json 勿手改；SESSION_LOG/HANDOFF task entry | `--check` summary 一致（域數/檔數/items）；app.html 政策範本 tab 連結 fetch 200；deploy 後 `/api/checklist-domains` 回 14 域 |
| Mobile shell scope / bottom-nav 變更（`mobile.js`/`mobile.css`；S164：手機底部導航增刪入口、新增 `#hash` 畫面、desktop-only 功能在手機的呈現方式） | `dev/PROJECT_MASTER_SPEC.md` §B.5 Mobile UI scope（nav entries + 每功能手機/桌面 呈現）；`README.md` 響應式/手機版範圍表；`CHANGELOG.md` Changed bullet；`CODEBASE_CONTEXT.md` AI Maintenance Log（+ Directory Map if a static asset/screen added）；SESSION_HANDOFF/LOG task entry。**不涉 desktop `app.html`/backend/凍結 JSON 合約**（純 mobile shell） | headless Chrome `--dump-dom`（fresh，bypass 快取）核實 mobile DOM：目標 shell + nav tab count + 新入口存在 + desktop-only 功能不在 mobile nav；其他手機畫面回歸；`git diff` 證 desktop/backend 零接觸；post-deploy 真機/headless live smoke |
| 資料庫更新日誌 `update_log.json` 維護（S171：首頁 `index.html` 「進入平台」旁 📋 icon → modal 渲染近期入庫/更新文件） | 每次有新源入庫 / 既有源重大更新時，於 `update_log.json` `entries` 頂部 append 一條（`date` / `action`〔新增·更新〕/ `title` / `desc` / 可選 `url`；newest-first、簡述即可）；SESSION_LOG task entry | `update_log.json` JSON valid；`index.html` 更新日誌 modal 載入並顯示新條目（headless/live render，非「載入中…」） |
| **臨時觀測 code 加落既有 backend route**（S198：`[route-probe]` 加落 `server.ts`，量度 `/api/search/channel-a` + `/combined` 有冇外部呼叫。適用於任何「為咗一次決定而暫時 instrument 生產」嘅改動） | `dev/SESSION_HANDOFF.md` Open Priorities **必須**寫明：(a) 呢段 code 係臨時、(b) **刪除責任同觸發條件**、(c) 觀察窗嘅**讀取日期**（受 log 保留期限制）、(d) 自測流量嘅識別標記（例如 `ua=s198-*`），讀數時要扣起；`dev/SESSION_LOG.md` task entry + 自檢證據；`dev/CODEBASE_CONTEXT.md` AI Maintenance Log。**code 內註釋必須自帶刪除指示同量度理由**，唔可以淨靠 handoff（handoff 會被重生，code 唔會） | 本機自檢：請求數 == log 行數（**要有 negative control：一條唔應該中嘅鄰近 route 必須零行**）；`npm run check` + `npm run build` exit 0；**部署後用帶序號嘅自測流量確認新版真係落咗地**（唔可以靠 `/health` 猜重啟 —— Render 零停機部署外部見唔到）；確認個儀器真係量度到目標訊號（**先問「呢個工具結構上量唔量得到？」，唔好信佢嘅沉默**） |

## Anti-pattern: No Matching Row

If your change has no matching row above:
- Do NOT skip silently — add the missing row first, then proceed
- Record the registry addition in SESSION_LOG under `Doc Sync: registry updated`
- Reason: a stale registry is worse than no registry (false safety net)
