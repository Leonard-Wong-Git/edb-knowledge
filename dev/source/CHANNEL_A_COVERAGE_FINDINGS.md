# Channel A 覆蓋量度 — 中期發現（S197, 2026-07-29）

**問題**：Channel A 嘅 455 條已核實事實，Channel B 文件語料食唔食得晒？食得晒就可以退役。

**答案（中期）**：我讀過嘅每一條，語料都有 —— 但真正嘅發現唔喺覆蓋率度。**Channel A 入面有相當一部分根本唔係政策事實，另有一部分同現行文件直接矛盾，而佢哋而家仲喺 `/api/search/channel-a` 服務緊。** 退役嘅理由比原本諗嘅強。

工具：`dev/source/channel_a_coverage.py`（self-test 34 項全 PASS，含兩條「故意整壞證明守衛會 FAIL」）
資料：`dev/source/coverage_runs/2026-07-29_s197_full_v2.json`（`_v2` = 修正中文數字後重判）

---

## 1. 量度設計上兩個必須講清楚嘅決定

### (a)「輸出完全一樣」唔係可用標準，改為「substance 覆蓋」

兩邊輸出永遠唔會一樣：Channel A 係**冇出處嘅裸句**（實查 `role_facts.json`：455 條分佈 47 個〔範疇×角色〕桶，每條淨係一個字串，`_source_refs` 只喺範疇層），Channel B 係原文 + URL + 頁碼。
所以標準係：**同一實質內容喺文件語料搵唔搵得返**。搵得返 = Channel B 嚴格優勝（同一件事，一邊有出處一邊冇）。

### (b) 必須剔走語料入面 Channel A 自己嘅鏡像 —— 否則個數係假嘅

`wiki_chunks` 16,062 條組成（本 session 逐類點過，總和相符）：

| content_type | 數量 | url | 算唔算文件語料 |
|---|---|---|---|
| `vault_extract` | 15,721 | 有 | ✅ |
| `footnote_curated` | 206 | 有 | ✅ |
| `approved_fact` | 109 | **空** | ❌ Channel A 鏡像 |
| `stat_fact` | 26 | **空** | ❌ 同族 |

嗰 109 條**逐字係 455 條嘅子集**（精確字串比對：109/109 命中，0 條外來）。唔剔走，攞事實去搵第一個命中就係佢自己 @0.828，**455/455 全覆蓋** —— 而個數會純粹係「問題就係佢自己嘅答案」嘅假象。

---

## 2. 量到嘅數（triage，唔係結論）

455 條，**0 傳輸錯誤**，桶數對數 OK：

| 桶 | 數量 | 意思 |
|---|---|---|
| `COVERED_ANCHORS` | 149 | 事實嘅所有硬錨點（金額／日數／百分比／條號）齊集喺**同一段**原文 |
| `PARTIAL_ANCHORS` | 26 | 部分錨點對到 → 矛盾／過時候選 |
| `NO_ANCHOR_HIT` | 32 | 一個錨點都對唔到 → 缺口候選 |
| `NO_ANCHORS` | 248 | 純質性描述，無錨點可對（其中 108 條有 lexical 支持、140 條未讀） |
| `ERROR` | 0 | — |

**錨點齊集必須喺同一段**，唔接受散落兩份文件 —— 兩個半命中砌埋一齊，正正係 S177 出街嗰個砌數失效模式。

**lexical 層自帶對照組**（每條事實同時對「為佢抽嘅段落」同「為第三者抽嘅段落」計分）：自身 median **8** vs 對照 median **0**；門檻 ≥8 收 231/455 正例、只漏入 **2/455** 對照。分佈分得開，所以呢層可用 —— 唔似 S195 個 cosine（敵意 0.632 vs 真命中 0.624 重疊，用唔到）。

---

## 3. 中途揪出自己把尺壞咗（已修，影響 71 條）

首輪報「8月15日前提交假期表」搵唔到錨點。打開 `g11` 一睇：「於每年**八月十五日**前向有關的總學校發展主任……呈交下一學年的學校假期表」—— **同一條規則，中文數字寫。**

首 29 條入面有 10 條係同一原因。加咗中文數字折算（`fold_cn_numerals`，處理「二零二五」= 2025 同「三十」= 30 兩種讀法）後離線重判，**71 條轉桶，`COVERED_ANCHORS` 100 → 149**。

⚠️ **方向性**：呢個 bug 令工具**系統性高報缺口**，而每一個假缺口都會變成「唔可以退 Channel A」嘅理由。修之前個數會令「保留 Channel A」睇落更有道理。

---

## 4. 人手讀完 55 條之後，型態同預期唔同

讀晒 `NO_ANCHOR_HIT` 29 條 + `PARTIAL_ANCHORS` 26 條。真缺口極少，反而揪出三類：

### 類 1 — 覆蓋咗，但檢索攞唔到（≠ 缺口）
直接原文搜尋推翻咗三條「疑似缺口」：

| Channel A 事實 | 語料實物 |
|---|---|
| 每所小學 35 萬「一筆過津貼」 | `edbc18_2023_pri_science`：「每所小學35萬的「一筆過津貼」」 |
| CPD 每三年 150 小時 | `tdtf_report_2019`：「每三年周期內參與不少於150 小時的專業發展活動」 |
| 非經常津貼不超過核准金額 50% | `coa_imc_1_19`：「可獲發不超過核准金額 50%的工程及設備資助款項」 |

**要分開兩件事**：語料有冇（退役問題）vs 檢索攞唔攞到（用戶體驗問題）。呢三條係後者，唔阻退役，但係 R8 reranking 嘅實證彈藥。

### 類 2 — Channel A 同現行文件**直接矛盾**（而家仲服務緊）

| Channel A 講 | 《學校行政手冊》(`sag_2025_11` 附錄9) 講 |
|---|---|
| 病假「滿六個月至三年最多有薪病假**36天**，三年以上同類最高36天」（兩條事實） | 「首年受聘時享有**28天**病假，其後每服務滿一年，可享有全年共**48天**；有薪病假最高可累積至**168天**」 |
| 非教學人員週年假期「滿一年至五年每年**18天**；五至十年**21天**；十年以上**24天**」 | 「服務滿1年至2年享**7天**有薪年假，其後每多服務1年可多獲1天，最高累積至**14天**」 |

「36天」呢個數喺語料入面搵唔到對應。**呢啲唔係覆蓋問題，係 Channel A 內容本身有問題**，而 `/api/search/channel-a` 今日實測仍然 HTTP 200 返 50 條結果。

### 類 3 — 根本唔係政策事實

逐條讀出嚟嘅，包括：
- **問卷題目**（收件人係學校本身）：「**貴校**需評分課程分配課時、教師專業能力及學生興趣的重要程度（1至5分）」
- **活動統計**：「2020/21–2022/23學年，領導工作坊52場，覆蓋534所小學，約2,000人參加」
- **殘缺片段**：「取得校長批准。」／「取得標書批核委員會批准。」／「2013年4月1日作出修訂。」
  （最後嗰條嘅來源已鎖定：`coa_imc_1_19` 每段結尾嘅**修訂註腳欄**「（2017年9月1日作出修訂）」被當成獨立事實抽咗出嚟）
- **出版註記**：「《小學指引》（2024）繁體中文版提供，英文版日後上載。」
- **抽取瑕疵**：「所有小學校長和教師均應閱覽上述指引及相**関**文件」（日文漢字「関」）

全庫 keyword 掃命中 **29 條**，但呢個係**下限**：keyword 掃係「去邊度睇」唔係證據，而且 first-match 分桶令「已過期死線」錯報成 0（嗰條被「問卷」型態先收咗）。我讀嘅 55 條入面命中率明顯高過 29/455。

---

## 5. 未確立嘅嘢（唔可以當已完成）

1. **140 條 `NO_ANCHORS` 且 lexical 弱嘅事實未逐條讀** —— 最終缺口清單未齊。
2. **仍然未決嘅個案**：採購記錄保存「3 個曆年」、全方位學習津貼「15%」上限、小學課時「23%/11%/16%」、實地考察「2-4 小時」。呢幾條 ilike 命中太多（232／1,946／1,357／264），要收窄再逐條開嚟讀。
3. **`COVERED_ANCHORS` 149 條只做過機械判斷**，未抽樣人手覆核。呢個係最會令結論好睇嘅一桶，抽樣必須做。
4. **消費面完全未查**：語料覆蓋答唔到「退咗會唔會整冧下游」。要 Leonard 喺 Render logs search `channel-a` 睇有冇真流量。

---

## 6. 對退役決定嘅中期判斷

**支持退役，而且理由比「Channel B 覆蓋到」更強**：Channel A 有一部分內容係問卷題、活動統計同抽取碎片（唔值得保留），另有至少 3 條同現行手冊矛盾嘅假期日數（**保留佢哋 = 繼續服務錯數**）。

**但仍未夠證據宣佈可以退**：缺口清單未齊（140 條未讀），而且退役範圍要講清楚 ——
`knowledge.json` **刪唔得**（`index.html:561` 公開首頁統計 + `q.html:233` 全部資料靠佢），退役範圍只係 `/api/search/channel-a`、`/api/search/combined`、`searchChannelA.ts`、`searchCombined.ts`、backend 開機載入 455 條、`app.html:2472` `runChannelA`、`role_facts.json`。

## 7. 落手指令

```bash
cd "/Users/leonard/Downloads/Claude Project/Claude-edb-knowledge/Draft" && python3 dev/source/channel_a_coverage.py --self-test
```
```bash
cd "/Users/leonard/Downloads/Claude Project/Claude-edb-knowledge/Draft" && python3 dev/source/channel_a_coverage.py --report dev/source/coverage_runs/2026-07-29_s197_full_v2.json --bucket NO_ANCHORS --sample 20
```
（`--run` 需要 `OPENAI_API_KEY` + `SUPABASE_SERVICE_KEY`；embedding 已快取喺 `coverage_runs/_embed_cache.json`，重跑唔使再畀錢 embed。）

---

## 8. S197 執行結果（同日，Leonard 批「先退服務面」→「逐條讀完 16 條先做」）

### 已做

1. **前端 Channel A 路徑移除**（commit `2d70ef7`，−109/+8 行）。已證摸唔到：`CHANNEL_OPTS` 只有一項，selector 寫住 `length > 1` 先 render。live 驗：served app.html 零殘留、Channel B 搜尋 200/7 條/synthesis 正常。
2. **eval 集補上盲點**（commit `5754c00`）：原 30 條**冇一條** expect `role_facts_*`、冇一條問「邊個負責」→ 剷走鏡像會量到假綠燈。加 4 條角色職責 query。
3. **9 條鏡像退出服務路徑**（commit `3ba92fe`）：`RETIRED_MIRROR_CHUNK_IDS`，逐條附已核實出處。**Supabase 零寫入**，刪一行即還原。

### 關鍵數字：機械判定 `CLEARED` 有 44% 撐唔住人手覆核

總帳提供 16 條候選，**只有 9 條過關**。被否決嘅 7 條同佢哋嘅失效模式：

| 事實 | 所謂「替代品」實際係咩 |
|---|---|
| 學校發展津貼 8 月發放 | 段落講午膳撥款同 IT 設備，完全無關 |
| 人文科科主任 30 小時 | 段落講**小學科學科**（30h/15h），主體錯 |
| 全方位學習津貼須用於… | 只係「五種基要學習經歷」嘅**定義**，唔係津貼用途規則 |
| 資訊科技統籌主任負責 IT 採購 | 段落講分工同資產管控，**無職責歸屬** |
| [課程統籌主任] 統籌人文科時間表 | 時間表有，**職責歸屬冇** |
| [活動主任] 統籌課外活動 | 段落講「其他學習經歷委員會」由**副校長**帶領 |
| 校長須確保…2027/28 取代常識科 | 「取代常識科」未證實 |

**規則：唔可以由總帳直接延長個 retired list，每條都要開段落嚟讀。** 已寫入 `searchChannelB.ts` 該常數上面嘅註釋。

### 一個我要記低嘅判斷錯誤

我曾提議**整批剷走 109 條**，理由係一條 query（採購門檻）見到無出處鏡像壓住 `g01` p.5。量埋成批之後：**93/109 冇已證替代品**，而且大部分係 `[角色] 負責…` 呢種語料唔會有嘅形態。live 實測「訓導主任 社工」鏡像佔 rank 0/1、語料最近似（`g16` p.17）講跨部門聯繫而唔講邊個負責。**由一個實例推去成批，係我推得太快。**

### QC

- eval 34 條 before→after：**PASS 23 / FAIL 0 / errors 0，0 blocking failures**，32 條完全相同
- 唯一 SET_ADDED = `procurement` 加入 `subvention_tips`（鏡像讓位俾自己嘅可引用正版，正是預期）
- `bus_escort` RANK_SHIFT：來源集不變、第 4/5 位對調，同校巴無關；符合設計容許嘅 ANN tie flip，**未獨立證實成因**
- tsc exit 0 ×2；Supabase 16,062 / registry 256 / v3.2.2 / 凍結合約全部零接觸

### 仍然未做

- **backend 兩條 route（`/api/search/channel-a`、`/api/search/combined`）未郁** —— 阻塞前置係 Render logs `channel-a` 流量，未有答案
- 總帳 172 條 `UNVERIFIED` + 107 條 `PROVISIONAL` 未讀；按上面 44% 覆核失敗率，呢兩桶**唔可以當可退**

---

## 5. S199（2026-07-30）—— 由「總帳」變成「一個要拍板嘅決定」

S199 由 Leonard 叫停 judge 工作、問返「Channel A 退役到底去到邊」而觸發。重讀總帳 + 打開段落核實之後，結論係：**唔使讀完 455 條先決定得到，因為真正嘅阻礙唔係「量度未夠」，而係一個資料模型缺口。**

### tier 全貌（總帳 TSV 實數，S199 核）

| tier | 數量 | 意思 |
|---|---|---|
| `UNVERIFIED` | 172 | 工具對唔到錨點，要人手讀 |
| `CLEARED` | 133 | 機械判「錨點齊集同一段」—— **44% 撐唔住人手覆核，唔可以當可退** |
| `PROVISIONAL` | 107 | 只有 lexical 支持、無錨點，比 CLEARED 更弱 |
| `NONPOLICY` | 29 | 唔係政策事實（問卷題／活動統計／殘片） |
| `HAND_VERIFIED` | 11 | 人手證實有出處 |
| `CONFLICT` | 3 | 同現行《學校行政手冊》直接矛盾（病假 36 天等，仲服務緊錯數） |

### 兩個唔同軸,唔可以撈埋（S199 釐清）

**軸一 — 455 條事實實際去咗邊：**
- **109 條**同時鏡像入咗 Channel B store（`content_type=approved_fact`，**url 全空 109/109**，護欄「答案必有出處」喺呢批身上穿窿；9 條已退服務路徑、100 條仲 serve 緊）
- **346 條**淨係活喺 Channel A（`role_facts.json`），**只可經 `channel-a` route 攞到**，Channel B store 根本冇

**軸二 — substance 有冇文件覆蓋（總帳量嘅嘢）：**係咪有一份**有 URL 有頁碼**嘅 Channel B 文件講到同一件事。

### 核心發現：職責歸屬類係資料模型缺口,唔係覆蓋率問題

總帳內含「負責／統籌／主任／督導」嘅事實有 **63 條**（37 UNVERIFIED + 17 PROVISIONAL + 9 CLEARED）；store 109 條鏡像入面 **53 條**係呢種形態（`[課程統籌主任] 負責統籌全校教師 CPD 計劃…`）。以 `role` 欄計，指名角色（非 `all_roles`）嘅事實共 **131 條**。

S199 逐條打開 retrieved passage 核實（唔信 tier）：事實係「**〔科主任〕須…／〔EO〕負責…／〔總務主任〕負責…**」——**指名邊個角色揹邊項職責**；而 top passage 全部只係**泛講程序**（「學校應確立正式程序…」「負責人員應…」），冇一段講明具名角色揹邊項職責。抽讀 12 條，型態一致。

**結論：Channel B 對呢批職責事實結構上覆蓋唔到 —— 語料唔會寫「〔某角色〕負責 X」。** 呢個唔係「量度未夠」或者「檢索攞唔到」，係文件本身冇呢種寫法。**所以 Channel A 冇得照原本諗法「B 蓋晒 A，安全刪」全退。**

### 個決定（只有 Leonard 揀得到；三選一，唔使讀完 455 條先揀）

嗰 ~131 條指名角色職責事實（含 63 條「負責」類）係孤兒。點處理係一個產品決定，唔係技術量度：

1. **接受蝕咗** —— 全退。「邊個負責」類事實喺平台上消失。最簡單，但蝕咗嘅嘢文件攞唔返。
2. **升做 Channel B 有出處事實** —— 攞嗰 ~131 條，揾／寫返佢哋嘅文件出處（多數源自 SAG／《資助學校採購程序指引》），以 `footnote_curated` + 真 URL 入庫。之後 Channel A 先真係冗餘。最多工，終態最好。
3. **保留殘餘** —— 前端退（已做）+ route 退大部分，但留個窄路淨服務職責事實。最亂。

**獨立於呢個決定、支持退 route 嘅一個理由：** 3 條 CONFLICT 假期日數（病假 36 天 vs 附錄9 28/48/168）已證唔喺 Supabase，只可經 `channel-a` route 攞到 → route 一日未拆，一日 serve 緊錯數。退 route 即刻止蝕。

### S199 冇做/守住嘅

- **零刪除、Supabase 零寫入、唔郁 route、唔延長 `RETIRED_MIRROR_CHUNK_IDS`。** 純讀 + 核實 + 記錄。
- 133 CLEARED 仍未逐條讀完（44% 不可信）；但**讀完都改變唔到上面個決定** —— 因為決定卡喺職責孤兒類，唔卡喺 CLEARED 桶嘅確實數。要精確化「可退幾多」先需要讀完,呢個係一個獨立、離線、embedding 已快取嘅後續工。
