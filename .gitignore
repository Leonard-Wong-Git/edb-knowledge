# macOS
.DS_Store
.AppleDouble
.LSOverride

# Session governance (internal AI agent files)
dev/SESSION_HANDOFF.md
dev/SESSION_LOG.md
AGENTS.md
CLAUDE.md
GEMINI.md
INIT.md
.claude/

# Uploads / temp
dev/uploads/
dev/edb-knowledge-admin-snapshot.json
dev/init_backup/
dev/_selftest_work/
dev/K1_API_SPEC.md
*.tmp
*.bak
tmp_fix.py
update_*.py

# Backend build / local env
backend/node_modules/
backend/dist/
backend/.env
backend/tsx
backend/edb-knowledge-platform-backend@*/

# Large generated files (exceed GitHub 100MB limit)
dev/knowledge/wiki_index.json

# Python cache
dev/vault/__pycache__/
**/__pycache__/
*.pyc

# Superseded files
landing.html

# Editor
.vscode/
.idea/

# MemPalace per-project files (issue #185)
mempalace.yaml
entities.json

# S163 P5 — local backup / intermediate pipeline artefacts (NOT deleted, just kept out
# of the release worktree). Manual safety copies + Workflow scratch outputs from the
# dev/checklists pipeline; reproducible, not source-of-truth.
*.bak_*
*.pre_remap
*.pre_fix
*.pre_qc_*
dev/checklists/_work/_distill_*_out.json
dev/checklists/_work/_rewrite_*_out.json
dev/checklists/_work/_rewrite_*_out_partial.json

# Option A (S188) — ingest-package generator STAGING output. Transient per-run
# packages (extract + package.json + execution_plan.json) awaiting human approval;
# reproducible from the dashboard feed, not source-of-truth. The generator script
# itself IS tracked.
dev/source/ingest_packages/
new_circulars.json

# Option A (S189, Phase 2) — control-plane scaffold for the future PRIVATE
# `edb-knowledge-ops` repo (approval queue + executor workflow template + secrets
# setup docs). Seeds a SEPARATE private repo; must not live tracked in this public
# repo. The dry-run executor script (dev/source/execute_ingest.py) itself IS tracked.
dev/source/ops/

# S197 Channel A coverage measurement — run artifacts are large (embed cache 14MB,
# run JSON ~4MB each) and fully reproducible from the tool. Findings live in
# dev/source/CHANNEL_A_COVERAGE_FINDINGS.md.
dev/source/coverage_runs/
